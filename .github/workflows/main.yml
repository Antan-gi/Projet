on:
  push:
    branches:
      - main  

name: 🚀 Deploy website on push

jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest

    steps:
    - name: 🚚 Get latest code
      uses: actions/checkout@v3
   
    - name: 📂 Sync files
      uses: SamKirkland/FTP-Deploy-Action@v4.3.4
      with:
        ftp-server: ftp-arcadialezoo.alwaysdata.net
        ftp-username: arcadialezoo
        ftp-password: ${{ secrets.ftp_password }}
        local-dir: ./  
        server-dir: /  
        protocol: ftps  
        port: 21  

